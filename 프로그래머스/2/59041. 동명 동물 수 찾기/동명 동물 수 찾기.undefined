-- NULL값 제외하려면 COUNT에 *말고, 컬럼명을 넣으면 된다.
SELECT NAME, COUNT(NAME) COUNT
FROM ANIMAL_INS
GROUP BY NAME
HAVING COUNT(NAME) >= 2
ORDER BY 1;

/*
-- 아래 SQL도 통과가 되긴 함
SELECT NAME, COUNT(*) COUNT
FROM ANIMAL_INS
-- WHERE NAME IS NOT NULL
GROUP BY NAME
HAVING COUNT(NAME) >= 2
ORDER BY 1;
*/